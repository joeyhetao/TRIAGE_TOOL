# xlog Agent Instructions

## Scope

- This workspace is for xlog business requirements only.
- Work in `/home/melo.liao/ai_tools/xlog` and its `xlog` Git branch.
- Do not modify the legacy `log_triage_tool` / triage implementation unless the
  user explicitly asks to do so in a separate request.

## Delivery

- Keep implementation, tests, schemas, and documentation consistent.
- Run the relevant verification before delivery.
- When the work is complete, commit the intended xlog changes and push them to
  the remote `xlog` branch. Build a release package when the change requires a
  release.
